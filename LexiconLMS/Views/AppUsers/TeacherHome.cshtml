@model TeacherHomeViewModel

@{
    ViewData["Title"] = "Index";
}
<div id="row-add-course" class="row justify-content-center align-items-center">
    <div class="col-sm-12 justify-content-center">
        <h1>Courses <a asp-controller="Courses" asp-action="Create"><img id="btn-create" src="/icons/create.svg" alt="Alternate Text" /></a></h1>
    </div>
</div>
@*<div id="row-add-course" class="row justify-content-center">
    <a asp-controller="Courses" asp-action="Create" id="btn-teacher-add-assignment" class="btn btn-primary"><i class="material-icons">add</i>Create New Course</a>
</div>*@

<!--Courses-->
<div class="container cont-section">
    <partial name="TeacherCourseListPartial" model="@Model.Courses" />
</div>

